<%= form_for(current_user.active_favorites.find_by(followed_id: @user.id),
            html: { method: :delete }, remote: true) do |f| %>
	<%= f.submit <%= t('users.Unfollow') %> class: "btn" %>
<% end %>