<div class="action-header">
  <div class="action-name">
    <p>● <%= t('users.Log_in') %></p>
  </div>

  <div class="action-detail">
    <p>・会員登録時に入力したメールアドレス、パスワードを入力して下さい。</p>
    <p>・出品や購入を行うためには、会員登録およびログインが必要です。</p>
  </div>
</div>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-5">

        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label :email, class: "label" %>
          <p class="control has-icons-left">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "email", class: "input" %>
            <span class="icon is-small is-left">
              <i class="fas fa-envelope"></i>
            </span>
          </p>
        </div>

        <div class="field">
          <%= f.label :password, class: "label" %>
          <p class="control has-icons-left">
            <%= f.password_field :password, autocomplete: "current-password", :placeholder => "password", class: "input" %>
            <span class="icon is-small is-left">
              <i class="fas fa-lock"></i>
            </span>
          </p>
        </div>

        <div class="actions">
          <%= f.submit t('users.Sign_in'), class: "button is-info" %>
        </div>
        <% end %>

        <%= render "devise/shared/links" %>

        <div class="field">
          <button class="btn-default google-login">
            <i class="fa fa-google" aria-hidden="true"></i>
            <%= link_to 'Googleでサインイン', user_google_oauth2_omniauth_authorize_path, style: "color:white;" %>
          </button>
        </div>

        <div class="field">
          <button class="btn-default facebook-login">
            <i class="fa fa-facebook-square" aria-hidden="true"></i>
            <%= link_to 'Facebookでサインイン', user_facebook_omniauth_authorize_path, style: "color:white;" %>
          </button>
        </div>
      </div>
    </div>
  </div>
</section>